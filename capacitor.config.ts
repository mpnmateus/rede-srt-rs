import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mateuspettenuzzo.redesrtrs',
  appName: 'rede-srt-rs',
  webDir: 'www'
};

export default config;
